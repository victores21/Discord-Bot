require("dotenv").config();

const config = {
  webUrl: "Url to scrap",
  channelMessageStart: "Discord channel id to start the bot",
  channelMessageToSend: "discord channel id to alert ",
  botToken: "bot token"
};

module.exports = { config };
